{
  "name": "storage-onramp",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/hardhat",
      "packages/nextjs"
    ]
  },
  "scripts": {
    "dev": "yarn workspace @storage-onramp/nextjs dev",
    "build": "yarn workspace @storage-onramp/nextjs build",
    "start": "yarn workspace @storage-onramp/nextjs start",
    "lint": "yarn workspace @storage-onramp/nextjs lint"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
